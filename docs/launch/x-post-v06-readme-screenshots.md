# X post — today: live README screenshots (May 19)

**What shipped today**
- Live UI captures replaced older hero / placeholder gallery images
- New README gallery: intervention, momentum, interaction intelligence, bridge
- `scripts/capture-readme-screenshots.mjs` for reproducible doc updates

**Image guide:** see `trajectory-drift/docs/launch/x-post-v06-readme-screenshots.md` (canonical).

| Post | Best image (native repo path) |
|------|-------------------------------|
| **Shorter (thesis only)** | `assets/launch/x-trajectory-infrastructure.png` — use drift copy or symlink; shows both repos + bridge JSON |
| **Native-only post** | `docs/screenshots/demo-hero.png` or `demo-intervention.png` |
| **Ecosystem thesis** | Prefer drift `assets/launch/x-trajectory-infrastructure.png` over `demo-hero.png` (hero is native-only) |

**Optional reply:** `docs/screenshots/demo-bridge.png` after ecosystem post with bridge JSON line.

---

## Post (recommended — ecosystem)

```
Today's ship: make GitHub show the real product.

trajectory-native: sustain momentum under entropy.
trajectory-drift: human + agent drift in one workspace.

Bridge JSON between them. Interaction starvation is a metric now.

READMEs caught up — live screenshots from the running apps (not mockups).
→ native: intervention · momentum · interaction intelligence · bridge
→ drift: unified workspace · multi-agent lanes · propagation diffs
→ capture scripts in both repos: npm run dev → node scripts/capture-readme-screenshots.mjs

https://github.com/higuseonhye/return
https://github.com/higuseonhye/drift

#BuildInPublic
```

**Attach:** `assets/launch/x-trajectory-infrastructure.png` or drift `dashboard-overview.png`

---

## Shorter (thesis only)

**Attach:** `assets/launch/x-trajectory-infrastructure.png` (**best fit** — not `demo-hero.png`)

```
trajectory-native: sustain momentum under entropy.
trajectory-drift: human + agent drift in one workspace.

Bridge JSON between them. Interaction starvation is a metric now.

https://github.com/higuseonhye/return
https://github.com/higuseonhye/drift

#BuildInPublic
```

**Optional reply:** `docs/screenshots/demo-bridge.png` or drift `assets/screenshots/dashboard-overview.png`

---

## Native-only

**Attach:** `docs/screenshots/demo-hero.png` or `demo-intervention.png`

```
Today's ship on trajectory-native: README screenshots that match v0.6.

Live captures — not mockups:
· intervention (what to do in reality)
· momentum surface
· interaction intelligence
· native ↔ drift bridge export

`scripts/capture-readme-screenshots.mjs` so the docs don't drift again.

https://github.com/higuseonhye/return

#BuildInPublic
```
